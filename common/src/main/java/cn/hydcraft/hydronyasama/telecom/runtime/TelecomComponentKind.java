package cn.hydcraft.hydronyasama.telecom.runtime;

/** Canonical telecom component kinds used by the runtime graph. */
public enum TelecomComponentKind {
  INPUT,
  OUTPUT,
  SIGNAL_BOX,
  DELAYER,
  TIMER
}
